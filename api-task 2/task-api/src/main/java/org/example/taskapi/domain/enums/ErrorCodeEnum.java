package org.example.taskapi.domain.enums;

public enum ErrorCodeEnum {
    ACCESS_DENIED,
    TASK_ALREADY_EXISTS,
    TASK_NOT_FOUND,
    TASK_LIST_EMPTY,
   USER_ALREADY_EXISTS,
    USER_NOT_FOUND,
    USER_LIST_EMPTY,
    UNAUTHORIZED;
}
